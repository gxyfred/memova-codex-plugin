{
  "mcpServers": {
    "memova": {
      "type": "http",
      "url": "https://api.memova.ai/mcp",
      "note": "Memova OAuth MCP server. Uses dynamic OAuth registration; if no scope is provided, Memova grants the supported MCP scopes during consent."
    }
  }
}
