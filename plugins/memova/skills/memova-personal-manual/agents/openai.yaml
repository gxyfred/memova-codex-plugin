interface:
  display_name: "Memova Personal Manual"
  short_description: "Create and publish your Memova Personal Manual."
  default_prompt: "Use $memova-personal-manual to generate and publish my Personal Manual."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "memova"
      description: "Memova OAuth MCP server for atomic Personal Manual Note publication."
      transport: "streamable_http"
      url: "https://api.memova.ai/mcp"
