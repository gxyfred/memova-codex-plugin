interface:
  display_name: "Memova Menu"
  short_description: "Choose a Memova workflow."
  default_prompt: "Use $memova-menu to open the Memova workflow menu."

policy:
  allow_implicit_invocation: true
